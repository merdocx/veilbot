# Инструкция по проверке V2Ray API

## ✅ Запросы выполнены с VeilBot сервера

**Время проверки:** $(date '+%Y-%m-%d %H:%M:%S')  
**Исходящий IP:** 46.151.31.105 или 77.246.105.29  
**Целевой сервер:** 38.180.192.10:8000

### Результаты:
- ✅ TCP соединение устанавливается
- ✅ HTTP запросы отправляются
- ❌ Ответа от сервера нет (таймаут через 10 секунд)

## 🔍 Что проверить на V2Ray сервере (38.180.192.10)

### Шаг 1: Мониторинг логов в реальном времени

**Откройте терминал на V2Ray сервере и выполните:**

```bash
sudo journalctl -u veil-xray-api -f | grep -E 'Request from IP|Access|46.151|77.246|POST|GET'
```

**Затем в другом терминале на VeilBot сервере выполните:**

```bash
# Запустите скрипт проверки
/root/veilbot/check_v2ray_api.sh

# Или выполните запросы вручную:
curl -v --max-time 10 http://38.180.192.10:8000/api
curl -v --max-time 10 -X POST "http://38.180.192.10:8000/api/keys" \
  -H "Authorization: Bearer 5a5-lxUnQF75TzeoObRGLZuHe27Qh_PnfxKPuxnplXA" \
  -H "Content-Type: application/json" \
  -d '{"name":"test"}'
```

### Шаг 2: Что искать в логах

**В логах должны появиться записи:**

#### ✅ Если запросы доходят до API:
```
🔍 Request from IP: 46.151.31.105 to /api
✅ Access granted
```

или

```
🔍 Request from IP: 77.246.105.29 to /api/keys
✅ Access granted
```

#### ❌ Если запросы блокируются:
```
⚠️ Access denied from IP: 46.151.31.105
Reason: IP not in whitelist
```

или

```
⚠️ Request blocked: IP 46.151.31.105 not allowed
```

#### ❌ Если запросы не доходят до API:
- Логи пустые - запросы блокируются на уровне файрвола/прокси до логирования
- Проверьте файрвол провайдера/облака
- Проверьте DDoS защиту

### Шаг 3: Проверка конфигурации IP Whitelist

**На V2Ray сервере проверьте конфигурацию whitelist:**

```bash
# Найдите конфигурационные файлы
grep -r "whitelist\|allow.*ip\|46.151.31.105\|77.246.105.29" \
  /etc/veil-xray-api/ \
  /opt/veil-xray-api/ \
  /var/lib/veil-xray-api/ \
  2>/dev/null

# Проверьте переменные окружения
sudo systemctl show veil-xray-api | grep -i ip
sudo systemctl show veil-xray-api | grep -i allow
```

**Убедитесь, что в whitelist добавлены оба IP:**
- `46.151.31.105`
- `77.246.105.29`

### Шаг 4: Проверка локальной доступности API

**На V2Ray сервере проверьте, работает ли API локально:**

```bash
# Проверка эндпоинта /api (должен работать без проверки IP)
curl -v http://127.0.0.1:8000/api

# Проверка эндпоинта /api/keys (требует токен)
curl -X POST "http://127.0.0.1:8000/api/keys" \
  -H "Authorization: Bearer 5a5-lxUnQF75TzeoObRGLZuHe27Qh_PnfxKPuxnplXA" \
  -H "Content-Type: application/json" \
  -d '{"name":"test_local"}'
```

**Если локально работает, а извне нет:**
- Проблема в IP whitelist
- Проблема в файрволе провайдера
- Проблема в маршрутизации

### Шаг 5: Проверка файрвола на V2Ray сервере

```bash
# Проверка UFW
sudo ufw status
sudo ufw status numbered | grep 8000

# Проверка iptables
sudo iptables -L -n -v | grep 8000
sudo iptables -L -n -v | grep 46.151.31.105
sudo iptables -L -n -v | grep 77.246.105.29

# Проверка firewalld (если используется)
sudo firewall-cmd --list-all | grep 8000
```

### Шаг 6: Проверка bind-адреса API

```bash
# Проверьте, на каком адресе слушает API
sudo netstat -tlnp | grep 8000
# или
sudo ss -tlnp | grep 8000

# Должно быть:
# 0.0.0.0:8000 или :::8000  ✅ Правильно
# 127.0.0.1:8000            ❌ Неправильно (только локальный доступ)
```

### Шаг 7: Проверка файрвола провайдера/облака

**В панели управления облаком проверьте:**

1. **Security Groups / Firewall Rules:**
   - Порт 8000 должен быть открыт для входящих соединений
   - Разрешите доступ с IP:
     - `46.151.31.105/32`
     - `77.246.105.29/32`

2. **DDoS защита:**
   - Может блокировать запросы автоматически
   - Добавьте IP в whitelist на уровне провайдера
   - Обратитесь в поддержку провайдера

3. **Network ACLs:**
   - Проверьте правила на уровне сети
   - Убедитесь, что порт 8000 не заблокирован

## 📊 Ожидаемое поведение

### Эндпоинт `/api`:
- ✅ Должен быть доступен без проверки IP
- ✅ Должен работать с любого IP
- ✅ Должен возвращать статус API

### Эндпоинт `/api/keys`:
- ✅ Требует проверки IP (whitelist)
- ✅ Должен работать только с IP из whitelist
- ✅ Требует токен авторизации

## 🔧 Возможные решения

### Если запросы не видны в логах:

1. **Проверьте файрвол провайдера** - запросы могут блокироваться до логирования
2. **Проверьте DDoS защиту** - может блокировать подозрительные запросы
3. **Проверьте прокси/балансировщик** - может быть перед API

### Если запросы видны, но блокируются:

1. **Добавьте IP в whitelist:**
   - `46.151.31.105`
   - `77.246.105.29`

2. **Проверьте формат whitelist** - может быть CIDR или точный IP

3. **Перезапустите сервис после изменения конфигурации:**
   ```bash
   sudo systemctl restart veil-xray-api
   ```

### Если запросы видны, но API не отвечает:

1. **Проверьте логи на ошибки:**
   ```bash
   sudo journalctl -u veil-xray-api -n 100 | grep -i error
   ```

2. **Проверьте нагрузку на сервер:**
   ```bash
   top
   htop
   ```

3. **Проверьте доступность ресурсов:**
   ```bash
   df -h
   free -h
   ```

## 📝 Скрипт для автоматической проверки

На VeilBot сервере создан скрипт `/root/veilbot/check_v2ray_api.sh` для автоматической проверки.

**Запуск:**
```bash
/root/veilbot/check_v2ray_api.sh
```

Скрипт выполнит все проверки и покажет результаты.

## 🎯 Следующие шаги

1. ✅ Выполните проверку логов на V2Ray сервере
2. ✅ Определите, доходят ли запросы до API
3. ✅ Если доходят - проверьте whitelist
4. ✅ Если не доходят - проверьте файрвол провайдера
5. ✅ Исправьте проблему и повторите проверку

## 📞 Контакты для помощи

Если проблема не решается:
- Проверьте документацию V2Ray API
- Обратитесь в поддержку провайдера облака
- Проверьте настройки DDoS защиты
