# Core dependencies
#
# Python: используйте 3.11.x для полной установки из этого файла.
# Python 3.12+: у aiogram 2.x стоит ограничение aiohttp<3.9; готовых wheels aiohttp 3.8.x
# для cp312 нет — pip пытается собрать из исходников и падает. Переход на Python 3.11
# или миграция на aiogram 3.x (другой API).
#
aiogram==2.25.1
fastapi==0.111.0
uvicorn==0.30.1
python-dotenv==1.0.1
passlib[bcrypt]==1.7.4
# passlib 1.7.4 ожидает bcrypt.__about__; в bcrypt 4.1+ это убрано — держим 4.0.x
bcrypt==4.0.1
pydantic==2.7.3
pydantic-settings==2.2.1
slowapi==0.1.9
jinja2==3.1.3
starlette==0.37.2

# Session signing for Starlette
itsdangerous==2.2.0

# HTTP client for V2Ray API (версия согласована с aiogram 2.x: >=3.8,<3.9)
aiohttp==3.8.6

# Security dependencies
certifi==2025.1.31
requests==2.32.3
urllib3==2.3.0

# Payment processing
yookassa==3.0.0

# Additional security
cryptography==44.0.1

# Async SQLite driver (used by payments repository)
aiosqlite==0.20.0

# System metrics used by memory optimizer and tests
psutil==5.9.8

# Test dependencies
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
pytest-mock==3.14.0
mypy==1.13.0
ruff==0.8.4
