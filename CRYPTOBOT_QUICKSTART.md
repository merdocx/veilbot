# Быстрый старт: Оплата криптовалютой через CryptoBot

## 📋 Шаг 1: Получение API токена от CryptoBot

1. Откройте Telegram → найдите бота **@CryptoBot**
2. Отправьте команду `/start`
3. Отправьте команду `/api` или `/new_api`
4. Следуйте инструкциям бота для создания токена
5. **Скопируйте токен** (он будет нужен в следующем шаге)

✅ **Проверка**: У вас есть токен вида `12345:ABCdef...`

---

## 📝 Шаг 2: Настройка переменных окружения

1. Откройте файл `.env` в корне проекта
2. Добавьте следующие строки (замените `ВАШ_ТОКЕН` на токен из шага 1):

```env
CRYPTOBOT_API_TOKEN=ВАШ_ТОКЕН
CRYPTOBOT_API_URL=https://pay.crypt.bot/api
CRYPTOBOT_DEFAULT_ASSET=USDT
CRYPTOBOT_DEFAULT_NETWORK=TRC20
```

3. Сохраните файл

✅ **Проверка**: Файл `.env` содержит строку `CRYPTOBOT_API_TOKEN=...`

---

## 🗄️ Шаг 3: Запуск миграций базы данных

Выполните команду:

```bash
python3 db.py
```

Ожидаемый результат:
```
Поле price_crypto_usd добавлено в tariffs
Добавлено полей для криптоплатежей: 5
```

✅ **Проверка**: Команда выполнена без ошибок

---

## 🌐 Шаг 4: Настройка webhook

### 4.1. Определите URL вашего webhook

Ваш webhook URL будет: `https://ВАШ_ДОМЕН/cryptobot/webhook`

**Примеры:**
- `https://veilbot.example.com/cryptobot/webhook`
- `https://api.yourdomain.com/cryptobot/webhook`

### 4.2. Настройте webhook через API

Создайте временный файл `setup_webhook.py`:

```python
import requests
import os
from dotenv import load_dotenv

load_dotenv()

API_TOKEN = os.getenv('CRYPTOBOT_API_TOKEN')
WEBHOOK_URL = "https://ВАШ_ДОМЕН/cryptobot/webhook"  # ЗАМЕНИТЕ НА ВАШ ДОМЕН

if not API_TOKEN:
    print("❌ Ошибка: CRYPTOBOT_API_TOKEN не найден в .env")
    exit(1)

response = requests.post(
    "https://pay.crypt.bot/api/setWebhook",
    headers={"Crypto-Pay-API-Token": API_TOKEN},
    json={"url": WEBHOOK_URL}
)

result = response.json()
if result.get('ok'):
    print("✅ Webhook успешно настроен!")
    print(f"URL: {WEBHOOK_URL}")
else:
    print("❌ Ошибка настройки webhook:")
    print(result)
```

Запустите:
```bash
python3 setup_webhook.py
```

**Важно:**
- Замените `ВАШ_ДОМЕН` на ваш реальный домен
- Убедитесь, что ваш сервер доступен из интернета
- Используйте HTTPS (не HTTP)

✅ **Проверка**: Скрипт вывел "✅ Webhook успешно настроен!"

### 4.3. Альтернатива: Ручная настройка через CryptoBot

Если API метод недоступен, webhook будет работать через периодическую проверку статуса (каждые 10 секунд). Это немного медленнее, но работает надежно.

---

## 💰 Шаг 5: Добавление цен в криптовалюте для тарифов

1. Запустите админ-панель (обычно `https://ваш-домен/login`)
2. Войдите в систему
3. Перейдите в раздел **"Тарифы"**
4. Для каждого тарифа, где нужна оплата криптой:
   - Нажмите кнопку **"Редактировать"** (иконка карандаша)
   - Найдите поле **"Цена в криптовалюте (USD)"**
   - Введите цену в долларах

**Как рассчитать цену:**
- Если тариф стоит 1000₽, а курс доллара 100₽
- То цена в USD = 1000 / 100 = 10.00

**Пример:**
- Тариф: "1 месяц" - 1000₽
- Цена в криптовалюте: 10.00 USD

5. Нажмите **"Сохранить изменения"**

✅ **Проверка**: В списке тарифов видна иконка криптовалюты с ценой в USD

---

## 🧪 Шаг 6: Тестирование

### 6.1. Перезапустите бота

```bash
# Остановите бота (Ctrl+C)
# Запустите заново
python3 bot.py
```

### 6.2. Тест создания платежа

1. Откройте Telegram бота
2. Нажмите **"Купить доступ"**
3. Выберите протокол (Outline или V2Ray)
4. Выберите страну
5. Выберите тариф (должен показывать цену в ₽ и $)
6. Нажмите **"₿ Криптовалюта (USDT)"**
7. Введите email
8. Должна появиться кнопка **"₿ Оплатить USDT"** со ссылкой

✅ **Проверка**: Появилась кнопка оплаты с ссылкой на CryptoBot

### 6.3. Тест оплаты (опционально)

⚠️ **Внимание**: Это реальная оплата! Используйте тестовый тариф с минимальной суммой.

1. Нажмите на кнопку **"₿ Оплатить USDT"**
2. Откроется CryptoBot в Telegram
3. Оплатите инвойс
4. Через несколько секунд бот должен отправить вам ключ

✅ **Проверка**: После оплаты бот автоматически выдал ключ

### 6.4. Проверка в админ-панели

1. Перейдите в раздел **"Платежи"**
2. Найдите платеж с provider="cryptobot"
3. Проверьте статус - должен быть "paid"

1. Перейдите в раздел **"Webhooks"**
2. Фильтр: provider="cryptobot"
3. Найдите запись с event="invoice_paid"
4. Проверьте result - должен быть "ok"

✅ **Проверка**: В админ-панели виден успешный платеж и webhook

---

## 🔍 Проверка работы системы

### Проверка 1: API токен работает

```bash
python3 -c "
import os
from dotenv import load_dotenv
from payments.services.cryptobot_service import CryptoBotService

load_dotenv()
service = CryptoBotService()
result = await service.get_me()
if result:
    print('✅ API токен работает!')
    print(f'Бот: {result}')
else:
    print('❌ Ошибка: API токен не работает')
"
```

### Проверка 2: Webhook доступен

Откройте в браузере (замените на ваш домен):
```
https://ваш-домен/cryptobot/webhook
```

Должна вернуться ошибка 405 (Method Not Allowed) - это нормально, webhook работает только для POST запросов.

Если ошибка 404 - проверьте, что роутер подключен.

---

## ⚠️ Частые проблемы

### Проблема: "CryptoBot service is not available"

**Решение:**
1. Проверьте `.env` файл - есть ли `CRYPTOBOT_API_TOKEN`
2. Перезапустите бота после изменения `.env`
3. Проверьте правильность токена

### Проблема: Кнопка "Криптовалюта" не появляется

**Решение:**
1. Проверьте, что для тарифа указана цена в криптовалюте (USD)
2. В списке тарифов должна быть иконка криптовалюты

### Проблема: После оплаты ключ не выдается

**Решение:**
1. Проверьте логи webhook в админ-панели (раздел Webhooks)
2. Проверьте, что есть доступные серверы для выбранного протокола/страны
3. Система автоматически проверяет статус каждые 10 секунд, подождите немного

### Проблема: Webhook не работает

**Решение:**
- Это не критично! Система автоматически проверяет статус инвойсов каждые 10 секунд
- Webhook просто ускоряет выдачу ключа (мгновенно vs 10 секунд)

---

## 📊 Мониторинг

После запуска отслеживайте:

1. **Логи webhook** (Админ-панель → Webhooks):
   - Фильтр: provider="cryptobot"
   - Проверяйте статус "ok" vs "error"

2. **Платежи** (Админ-панель → Платежи):
   - Фильтр: provider="cryptobot"
   - Отслеживайте статусы: pending → paid

3. **Логи бота**:
   - Ищите ошибки с префиксом `[CRYPTOBOT]`

---

## ✅ Финальный чеклист

- [ ] API токен получен и сохранен в `.env`
- [ ] Миграции БД выполнены (`python3 db.py`)
- [ ] Webhook настроен (или будет работать через polling)
- [ ] Цены в криптовалюте добавлены для тарифов
- [ ] Бот перезапущен после изменений
- [ ] Протестировано создание инвойса
- [ ] (Опционально) Протестирована оплата и выдача ключа

---

## 🎉 Готово!

Если все шаги выполнены, оплата криптовалютой должна работать!

**Что дальше:**
- Мониторьте первые платежи
- Настройте цены для всех тарифов
- Рассмотрите добавление других криптовалют (BTC, ETH)

