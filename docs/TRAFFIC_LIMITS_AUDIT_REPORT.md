# Отчет об использовании лимитов трафика в проекте

**Дата проверки:** 2025-12-17  
**Статус архитектуры:** Все лимиты должны храниться в подписках, а не в ключах

## Архитектура лимитов трафика

### Правильная архитектура:
- ✅ **Лимиты хранятся в подписках:** `subscriptions.traffic_limit_mb` или берется из `tariffs.traffic_limit_mb`
- ✅ **Использование трафика в ключах:** `v2ray_keys.traffic_usage_bytes` - каждый ключ отслеживает свой трафик
- ✅ **Агрегация на уровне подписки:** Суммируется `traffic_usage_bytes` всех ключей подписки
- ✅ **Проверка лимита:** На уровне подписки, не отдельных ключей

### Неправильно (legacy):
- ❌ Хранение `traffic_limit_mb` в таблицах `keys` и `v2ray_keys`
- ❌ Проверка лимитов по отдельным ключам (для подписок)

## ❌ Проблемные места

### 1. INSERT traffic_limit_mb в ключи

#### 1.1 bot/services/key_management.py
- **Строка 730**: INSERT INTO keys с traffic_limit_mb (при смене протокола)
- **Строка 836**: INSERT INTO v2ray_keys с traffic_limit_mb (при смене протокола)
- **Строка 1069**: INSERT INTO keys с traffic_limit_mb (при смене страны)
- **Строка 1383**: INSERT INTO keys с traffic_limit_mb (при смене протокола)
- **Строка 1657**: INSERT INTO keys с traffic_limit_mb (при смене страны)
- **Строка 2026**: INSERT INTO keys с traffic_limit_mb (при перевыпуске)

**Проблема:** Лимит должен браться из подписки, а не вставляться в ключ.

#### 1.2 bot/services/background_tasks.py
- **Строка 479**: INSERT INTO keys с traffic_limit_mb (standalone ключи)
- **Строка 582**: INSERT INTO v2ray_keys с traffic_limit_mb и expiry_at (standalone ключи)

**Проблема:** Это legacy код для standalone ключей, но все равно не должен устанавливать лимит.

#### 1.3 payments/services/subscription_purchase_service.py
- **Строка 1225**: INSERT INTO v2ray_keys с traffic_limit_mb (при создании подписки)
- **Строка 1304**: INSERT INTO keys с traffic_limit_mb и expiry_at (при создании подписки)

**Проблема:** Лимит уже есть в подписке, не нужно дублировать в ключах.

#### 1.4 bot/services/subscription_service.py
- **Строка 724**: INSERT INTO keys с traffic_limit_mb (при создании подписки)

**Проблема:** Лимит уже есть в подписке.

### 2. UPDATE traffic_limit_mb в ключах

#### 2.1 payments/services/subscription_purchase_service.py
- **Строка 574-577**: UPDATE v2ray_keys SET traffic_limit_mb = ?
- **Строка 585-588**: UPDATE keys SET traffic_limit_mb = ?

**Проблема:** Лимит должен обновляться только в подписке. Ключи используют лимит из подписки.

#### 2.2 bot/services/key_management.py
- **Строка 343-344**: UPDATE keys SET traffic_limit_mb = ? (при продлении с альтернативным сервером)
- **Строка 401-402**: UPDATE v2ray_keys SET traffic_limit_mb = ? (при продлении с альтернативным сервером)
- **Строка 425-426**: UPDATE keys SET traffic_limit_mb = ? (при продлении)
- **Строка 438-439**: UPDATE v2ray_keys SET traffic_limit_mb = ? (при продлении)
- **Строка 859-867**: UPDATE v2ray_keys SET traffic_limit_mb (backfill при смене протокола)

**Проблема:** Лимит должен браться из подписки, не нужно обновлять в ключах.

#### 2.3 bot/services/background_tasks.py
- **Строка 604-613**: UPDATE keys SET traffic_limit_mb (backfill для standalone ключей)
- **Строка 599-619**: UPDATE v2ray_keys SET traffic_limit_mb (backfill)

**Проблема:** Это legacy код, но все равно не должен обновлять лимит в ключах.

### 3. Fallback в subscription_repository.py

#### 3.1 app/repositories/subscription_repository.py
- **Строки 747-759**: Fallback, который берет лимит из ключей, если его нет в подписке

**Проблема:** Это legacy fallback для старых данных. Новые ключи не должны иметь traffic_limit_mb.

### 4. Использование traffic_limit_mb из ключей в логике

#### 4.1 admin/routes/keys.py
- **Строки 254-262**: Используется traffic_limit_mb из ключа для legacy ключей без подписки

**Статус:** ✅ Это правильно - для legacy ключей без subscription_id можно использовать лимит из ключа.

### 5. Использование удаленных полей (КРИТИЧНО)

#### 5.1 bot/services/key_management.py
Код пытается использовать поля, которые были удалены из таблицы v2ray_keys:
- **traffic_over_limit_at** - удалено из v2ray_keys
- **traffic_over_limit_notified** - удалено из v2ray_keys

**Проблемные места:**
- **Строка 139**: SELECT traffic_over_limit_at, traffic_over_limit_notified
- **Строка 836**: INSERT INTO v2ray_keys с traffic_over_limit_at, traffic_over_limit_notified
- **Строка 1160**: INSERT INTO v2ray_keys с traffic_over_limit_at, traffic_over_limit_notified
- **Строка 1444**: INSERT INTO v2ray_keys с traffic_over_limit_at, traffic_over_limit_notified
- **Строка 1748**: INSERT INTO v2ray_keys с traffic_over_limit_at, traffic_over_limit_notified
- **Строка 2143**: INSERT INTO v2ray_keys с traffic_over_limit_at, traffic_over_limit_notified

**Проблема:** Эти поля были удалены миграцией, но код все еще пытается их использовать. Это вызовет SQL ошибки.

#### 5.2 payments/services/subscription_purchase_service.py
- **Строка 1225**: INSERT INTO v2ray_keys - может содержать expiry_at (который удален)

**Проблема:** Проверить, используется ли expiry_at.

## 📊 Статистика

- **Всего INSERT с traffic_limit_mb:** 9 мест
- **Всего UPDATE traffic_limit_mb:** 11 мест
- **Использование удаленных полей (traffic_over_limit_at/notified):** 6 мест в INSERT
- **SELECT удаленных полей:** 1 место
- **Проблемных мест:** ~27 (без учета legacy fallback)

## 🔄 Что нужно исправить

### Принципы исправления:

1. **Убрать traffic_limit_mb из INSERT запросов** - лимит берется из подписки
2. **Убрать UPDATE traffic_limit_mb** - лимит управляется через подписку
3. **Оставить traffic_usage_bytes в ключах** - это нормально, каждый ключ отслеживает свой трафик
4. **Проверки лимитов делать на уровне подписки** - не по отдельным ключам

### Исключения (legacy):

- Ключи без `subscription_id` (standalone) могут иметь `traffic_limit_mb` для обратной совместимости
- Fallback в `subscription_repository` можно оставить для старых данных

## ⚠️ Важные замечания

1. **traffic_usage_bytes в ключах - это нормально** - это использование конкретного ключа, не лимит
2. **Лимит должен быть один на подписку** - берется из `subscriptions.traffic_limit_mb` или `tariffs.traffic_limit_mb`
3. **Все ключи подписки используют один лимит** - агрегируется трафик всех ключей
4. **При создании ключей для подписки** - не нужно устанавливать traffic_limit_mb, лимит уже в подписке
5. **traffic_over_limit_at и traffic_over_limit_notified** - в v2ray_keys могут быть для legacy ключей, но для подписок используются аналогичные поля в subscriptions

## ✅ Правильные места (не требуют изменений)

1. **monitor_subscription_traffic_limits()** - правильно проверяет лимиты на уровне подписок
2. **admin/routes/keys.py** - правильно использует лимит из подписки для ключей с subscription_id
3. **subscription_repository.get_subscription_traffic_limit()** - правильно берет лимит из подписки/тарифа
4. **traffic_usage_bytes в ключах** - правильно используется для агрегации на уровне подписки

