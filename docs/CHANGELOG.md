# Changelog

Все важные изменения в проекте VeilBot документируются в этом файле.

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/),
и проект следует [Semantic Versioning](https://semver.org/lang/ru/).

## [Unreleased]

## [2.4.38] - 2026-02-21

### Исправлено
- **Колонка expiry_at удалена из таблиц keys и v2ray_keys**: убраны все обращения к колонке в INSERT/SELECT; срок действия ключей берётся из `subscriptions.expires_at` через JOIN. Исправлены: background_tasks.py, get_user_info.py, purchase.py; миграции в db.py (migrate_add_server_cascade_to_keys, migrate_fix_v2ray_keys_foreign_keys) пересоздают таблицы без expiry_at.

## [2.4.37] - 2026-02-21

### Исправлено
- **Пробные подписки без ключей (#496, #499 и др.)**:
  - В платёжном сервисе при создании ключей для новой подписки учитывается текущая подписка при проверке доступа к серверам с `access_level = 'paid'` (у новой подписки был `expires_at = now`, из-за чего `has_active_subscription` оказывался false и список серверов для ключей пустел).
  - В `SubscriptionService.create_subscription` при проверке `has_active_subscription` для доступа к paid-серверам также учитывается подписка с `id = subscription_id`.
- **Деактивация подписки при ошибке создания ключей убрана**: при `created_keys == 0` подписка больше не деактивируется (ни в `SubscriptionService`, ни в `_create_subscription`), чтобы ключи можно было доставить позже (крон, скрипт, админ).
- **Синхронизация ключей (крон и админка)**:
  - Загружаются только активные серверы (`active = 1`); неактивные/удалённые не участвуют в создании ключей и не вызывают API.
  - Для создания ключей серверы больше не исключаются по результату проверки доступности API (медленный ответ не приводит к нулю созданных ключей).
  - Сообщение в логе при отсутствии Outline-сервера уточнено.

## [2.4.35] - 2026-02-14

### Исправлено
- **Уведомление админу при оплате не доходило (вебхуки)**: при отправке через Telegram API fallback сообщение админу отправляется как plain text (без Markdown), чтобы спецсимволы в payment_id или названии тарифа не вызывали 400 от API; в `_send_via_telegram_api` параметр `parse_mode` передаётся в API только при непустом значении; добавлено логирование перед отправкой (admin_id, payment_id).

## [2.4.34] - 2026-02-14

### Исправлено
- **Подписка без ключей, срок просрочен, нет completed**:
  - Статус completed выставляется сразу после привязки платежа к подписке (`update_subscription_id`), до расчёта expires_at и создания ключей — при любом исключении дальше платёж остаётся completed
  - В расчёт срока для новой подписки добавлен `current_tariff`: при отсутствии длительности из репозитория используется `duration_sec` из текущего тарифа, чтобы срок не оставался «сейчас» (просрочен)
- **Тест test_init_db_with_migrations_creates_core_tables**: колонка `subscription_id` добавлена в миграцию `migrate_extend_payments_schema()`, чтобы индекс `idx_payments_subscription_id` создавался по существующей колонке

## [2.4.33] - 2026-02-14

### Исправлено
- **Подписка создана, ключи не создались, статус не completed**:
  - В `_create_keys_for_subscription` при отсутствии V2Ray-серверов переменная `v2ray_results` не определялась — добавлена инициализация `v2ray_results = []`, чтобы не возникал NameError и создание Outline-ключа работало
  - Статус платежа переводится в completed до отправки уведомлений; уведомления админу и пользователю обёрнуты в try/except с логированием, чтобы сбой отправки не оставлял платёж без статуса completed

## [2.4.32] - 2026-02-14

### Изменено
- **Снижение блокировок event loop (узкие места)**:
  - Добавлен индекс `idx_payments_subscription_id` для запросов по `payments.subscription_id`
  - `reset_subscription_traffic`: чтение ключей и обновление БД перенесены в `asyncio.to_thread()`
  - `monitor_subscription_traffic_limits`: все обращения к sync БД выполняются в executor
  - Страница расхождений и POST fix: расчёт `_calculate_subscription_discrepancies` в executor
  - Синхронизация ключей (POST /subscriptions/sync-keys): запуск в отдельном потоке с собственным event loop

## [2.4.30] - 2026-01-26

### Исправлено
- ✅ **Уведомления админу при оплате через вебхуки**:
  - Вебхуки (YooKassa, Platega, CryptoBot) обрабатываются в процессе админки, где `get_bot_instance()` всегда `None`
  - Добавлен fallback: при `get_bot_instance() is None` уведомления отправляются через Telegram API (aiohttp)
  - Уведомления о покупках и о новых расхождениях теперь доходят при оплате через вебхуки

- ✅ **Редактирование сервера в админке (504 при снятии «Активен»/«Доступен к покупке»)**:
  - Удаление ключей при деактивации сервера вынесено в фон (`asyncio.create_task`), ответ отдаётся сразу
  - Для V2Ray и Outline добавлены таймауты 5 с на каждое удаление ключа, чтобы не зависать на недоступных серверах

### Изменено
- Уведомления о расхождениях: при `bot is None` используется отправка через Telegram API

## [2.4.29] - 2026-01-26

### Добавлено
- ✅ **Уведомления администратору о покупках подписок**:
  - Администратор получает уведомления в бот обо всех успешных покупках и продлениях подписок
  - Уведомление включает: ID пользователя, ID подписки, тариф, сумму, способ оплаты, срок действия, тип (новая/продление), ID платежа
  - Уведомления отправляются во всех местах создания/продления подписки:
    - `process_subscription_purchase()` - основной метод обработки покупки
    - `_extend_subscription()` - продление существующей подписки
    - `_send_purchase_notification_for_existing_subscription()` - уведомление для существующей подписки
    - `_create_subscription_as_renewal()` - создание подписки как продление бесплатного ключа

- ✅ **Уведомления администратору о новых расхождениях в подписках**:
  - Администратор получает уведомления в бот только о новых расхождениях (не дублируются)
  - Уведомление включает: ID пользователя, ID подписки, тариф, расхождение в днях, текущий и расчетный сроки, количество платежей и бонусов
  - Создана таблица `discrepancy_notifications` для отслеживания отправленных уведомлений
  - Уведомления отправляются при открытии страницы расхождений в админке

## [2.4.28] - 2026-01-26

### Изменено
- ✅ **Убрана информация по нажатиям Start из графика "Динамика по дням"**:
  - График теперь показывает только активные подписки и платные подписки
  - Убраны линия, градиент и легенда для "Нажали «Start»"
  - Блок "Нажали «Start»" остался в верхней части страницы (stats-grid) для отображения текущего числа нажатий

## [2.4.27] - 2026-01-23

### Исправлено
- ✅ **Исправление установки лимита трафика при оплате подписки**:
  - Исправлена проблема с получением `traffic_limit_mb` из тарифа (убрано использование `or 0`, которое теряло значение)
  - Исправлены подписки #316 и #322: установлен правильный лимит трафика (12288000 MB вместо 9216 MB)
  - Добавлена явная проверка на `None` при получении `traffic_limit_mb` из тарифа
- ✅ **Исправление деактивации тарифов в админке**:
  - Исправлена обработка чекбокса `is_archived` в форме FastAPI
  - Теперь значение чекбокса получается напрямую из формы через `form_data.get()`
  - Деактивация тарифов теперь сохраняется после обновления страницы
- ✅ **Исправление двойного добавления длительности тарифа при создании подписки**:
  - При создании новой подписки устанавливается `expires_at = now` вместо `now + duration_sec`
  - Реальная дата окончания пересчитывается в `process_subscription_purchase()` через `_calculate_subscription_expires_at()`
  - Это предотвращает двойное добавление длительности тарифа (проблема подписки #349)

### Изменено
- ✅ **Скрытие способа оплаты Platega в боте, если он отключен в тарифах**:
  - Функция `get_payment_method_keyboard()` теперь проверяет наличие тарифов с включенными способами оплаты
  - Показываются только те способы оплаты, которые доступны хотя бы для одного тарифа
- ✅ **Замена подсчета истекших подписок на платные активные не VIP подписки**:
  - На странице subscriptions в верхнем блоке теперь отображается количество платных активных не VIP подписок
  - Платные: есть хотя бы один completed платеж ИЛИ тариф с `price_rub > 0`
  - Активные: `is_active = 1` и `expires_at > now_ts`
  - Не VIP: `expires_at < VIP_EXPIRES_AT` (4102434000)

## [2.4.26] - 2026-01-19

### Исправлено
- ✅ **Критические исправления процесса покупки и продления подписок**:
  - Исправлена проблема с обновлением `tariff_id` при продлении подписки (теперь обновляется всегда)
  - Исправлена проблема с обновлением `tariff_id` в методе `_get_or_create_subscription()` при использовании существующей подписки
  - Добавлена проверка VIP статуса во всех критических методах:
    - `process_subscription_purchase()` - VIP подписки не изменяются при продлении
    - `_extend_subscription()` - VIP подписки сохраняют VIP_EXPIRES_AT
    - `_get_or_create_subscription()` - VIP пользователи получают VIP подписки
  - Исправлена подписка #257: установлен правильный тариф из платежа
  - Реферальный бонус теперь всегда сохраняется при продлении подписки

### Добавлено
- ✅ **Скрипты для анализа и исправления подписок**:
  - `scripts/find_subscriptions_9gb_not_100gb.py` - поиск подписок с лимитом 9 ГБ вместо 100 ГБ
  - `scripts/fix_subscriptions_9gb_to_100gb.py` - исправление подписок с неправильным лимитом
  - `scripts/analyze_subscription_traffic_limit_issues.py` - анализ проблем с лимитом трафика
  - `scripts/analyze_subscription_purchase_flow.py` - анализ процесса покупки и продления
  - `scripts/analyze_vip_and_referral_bonus.py` - анализ учета VIP статуса и реферального бонуса

### Документация
- ✅ Добавлена документация по анализу и исправлению проблем с подписками:
  - `docs/SUBSCRIPTION_TRAFFIC_LIMIT_ANALYSIS.md` - анализ проблем с лимитом трафика
  - `docs/SUBSCRIPTION_TRAFFIC_LIMIT_FIX.md` - описание исправлений лимита трафика
  - `docs/SUBSCRIPTION_PURCHASE_FLOW_ANALYSIS.md` - анализ процесса покупки и продления
  - `docs/VIP_AND_REFERRAL_BONUS_ANALYSIS.md` - анализ учета VIP статуса и реферального бонуса
  - `docs/VIP_AND_REFERRAL_BONUS_FIXES.md` - описание исправлений VIP статуса

## [2.4.25] - 2026-01-18

### Исправлено
- ✅ **Критическая ошибка: лимит трафика не обновлялся при оплате подписки**:
  - Исправлена логика обновления `traffic_limit_mb` в методе `_extend_subscription`
  - Теперь лимит трафика обновляется всегда: при покупке (`is_purchase=True`), при продлении (`is_purchase=False`), и при ручной установке срока (`is_manually_set=True`)
  - Исправлены подписки #295 и #286: установлен правильный лимит трафика из тарифа (102400 MB)

## [2.4.24] - 2026-01-18

### Изменено
- ✅ **Фильтр "Платные" на странице подписок**:
  - Заменен checkbox на select (как на странице пользователей)
  - Фильтр автоматически применяется при изменении через live-search.js
  - Исправлена обработка параметра paid (теперь str, как vip_filter)

## [2.4.23] - 2026-01-18

### Добавлено
- ✅ **Фильтр "Платные" на странице подписок**:
  - Добавлен чекбокс для фильтрации платных подписок (не VIP, с price_rub > 0)
  - Фильтр работает совместно с поиском
  - Пагинация сохраняет состояние фильтра

### Исправлено
- ✅ **Исправлена критическая ошибка обработки платежей за подписки**:
  - Устранена ошибка `UnboundLocalError: local variable 'subscription_id_retry' referenced before assignment`
  - Исправлены ошибки отступов в блоках `async with` в `subscription_purchase_service.py`
  - Теперь все платежи за подписки обрабатываются корректно

## [2.4.22] - 2026-01-18

### Добавлено
- ✅ **Отображение платных подписок в динамике дашборда**:
  - Добавлена зеленая линия "Платные подписки" на графике "Динамика по дням" в админке
  - Показывается количество активных платных подписок (не VIP, с price_rub > 0) на конец каждого дня
  - Добавлено поле `paid_subscriptions` в таблицу `dashboard_metrics`
  - Данные автоматически обновляются при каждой загрузке дашборда

## [2.4.21] - 2026-01-27

### Оптимизировано
- ✅ **Оптимизация API-запросов к VPN серверам**:
  - Параллельное создание ключей для подписок через `asyncio.gather`
  - Добавлен `ServerClientPool` для переиспользования соединений к серверам
  - Время создания ключей на всех серверах сокращено в 5-10 раз
  - Устранено избыточное создание клиентов для каждого запроса

### Исправлено
- ✅ **Исправлено обновление статуса на completed при retry webhook**:
  - Добавлено обновление статуса на `completed` в блоке retry, когда ключи уже созданы
  - Решает проблему, когда платежи оставались в статусе `paid` после успешной обработки

## [2.4.20] - 2026-01-17

### Изменено
- ✅ **Изменена логика расчета expires_at при продлении подписки**:
  - При создании новой подписки - пересчет на основе всех платежей (как было)
  - При продлении существующей подписки - простое продление: текущий expires_at + длительность нового платежа
  - Это более интуитивно и соответствует ожиданиям пользователей

### Добавлено
- ✅ **Retry механизм для создания ключей при таймаутах**:
  - До 3 попыток создания ключа на каждом сервере при таймауте
  - Задержка 2 секунды между попытками
  - Retry только для таймаутов (не для других ошибок)
  - Закрытие клиента перед следующей попыткой для чистого переподключения
  - Решает проблему частых пропусков создания ключей на сервере #19 из-за таймаутов

## [2.4.19] - 2026-01-17

### Изменено
- ✅ **Рефакторинг флоу создания ключей для подписок**:
  - Создан единый метод `_create_keys_for_subscription` для создания ключей на всех серверах
  - Устранено дублирование кода создания ключей (было в 3 местах, теперь в 1)
  - Ключи автоматически создаются в `_get_or_create_subscription` при создании новой подписки
  - При retry webhook ключи создаются автоматически, если их нет
  - Удален временный костыль из `process_subscription_purchase` (около 90 строк дублирующего кода)
  - Флоу создания подписки и ключей стал единообразным и надежным

### Исправлено
- ✅ **Исправлено создание ключей при retry webhook**:
  - Если подписка существует, но ключей нет, они создаются автоматически
  - Убрано сообщение "Keys should be created manually" - теперь ключи создаются автоматически

## [2.4.18] - 2026-01-17

### Добавлено
- ✅ **Кнопка копирования ссылки подписки на странице подписок**:
  - Добавлена колонка "Ссылка" с кнопкой копирования (аналогично странице ключей)
  - Кнопка копирует ссылку вида `https://veil-bot.ru/api/subscription/{token}`
  - Реализована функция `copyToClipboard` в `subscriptions.js`
  - Используется современный Clipboard API с fallback на `document.execCommand`

### Исправлено
- ✅ **Исправлены отступы в `subscription_purchase_service.py`**:
  - Исправлены неправильные отступы в блоке получения платежей (строки 173-183)
  - Удалено дублирование `async with open_async_connection`

## [2.4.17] - 2026-01-17

### Исправлено
- ✅ **Исправлено обновление tariff_id при продлении подписки**:
  - Перенесено обновление subscription_id в платеже ПЕРЕД пересчетом expires_at
  - Теперь SQL-подзапрос в _recalculate_and_update_subscription_expires_at корректно находит текущий платеж
  - tariff_id автоматически обновляется из последнего completed платежа при каждом продлении
  - Исправлена проблема, когда tariff_id оставался пустым после продления подписки

## [2.4.16] - 2026-01-17

### Исправлено
- ✅ **Исправлена работа кнопки "Исправить" на странице расхождений**:
  - Переделана реализация с JavaScript fetch на стандартную HTML форму (как в других местах админки)
  - Кнопка теперь работает через POST форму с `data-confirm` для подтверждения
  - Endpoint изменен с JSONResponse на RedirectResponse для консистентности
  - Добавлено отображение сообщений об успехе/ошибке через сессию
  - Убрана сложная JavaScript логика, используется стандартная обработка форм через `common.js`

## [2.4.15] - 2026-01-17

### Добавлено
- ✅ **Кнопка исправления расхождений на странице discrepancies**:
  - Добавлена кнопка "Исправить" рядом с каждым расхождением в таблице
  - POST endpoint `/subscriptions/discrepancies/fix/{subscription_id}` для автоматического исправления
  - Автоматическое обновление `expires_at` подписки на расчетное значение
  - Обновление сроков всех связанных ключей
  - Подтверждение действия перед исправлением
  - Автоматическое обновление страницы после успешного исправления

### Улучшено
- ✅ **Удобство исправления расхождений**:
  - Одно нажатие кнопки для исправления расхождения
  - Визуальная обратная связь (индикатор загрузки, сообщения об успехе/ошибке)
  - Логирование всех действий исправления

## [2.4.14] - 2026-01-17

### Изменено
- ✅ **Упрощение логики обработки подписок**:
  - Упрощенная логика определения покупки vs продления (3 проверки вместо 28+)
  - Проверка статуса `completed` (быстрая проверка)
  - Проверка `subscription_id` (защита от retry webhook)
  - Определение покупки/продления (есть подписка → продление, нет → покупка)
  - Пересчет `expires_at` на основе всех платежей вместо продления
  - Универсальное уведомление "Подписка обновлена!" для всех случаев

### Добавлено
- ✅ **Новые функции расчета подписок**:
  - `_calculate_subscription_expires_at` - пересчет с учетом разных тарифов и реферальных бонусов
  - `_calculate_referral_bonuses` - расчет реферальных бонусов (30 дней × количество рефералов)
  - `_get_or_create_subscription` - получение или создание подписки (защита от race condition)
  - `_recalculate_and_update_subscription_expires_at` - атомарное обновление `expires_at`
  - `_send_universal_notification` - универсальное уведомление для всех платежей
  - `_send_outline_backup_keys_notification` - отправка Outline ключей только при первом платеже

### Исправлено
- ✅ **Учет разных тарифов и реферальных бонусов**:
  - Все платежи учитываются при расчете `expires_at` (с разными тарифами)
  - Реферальные бонусы добавляются: 30 дней × количество рефералов с `bonus_issued=1`
  - VIP подписки не пересчитываются (остаются `VIP_EXPIRES_AT`)

### Миграция
- ✅ **Добавлено поле `notification_sent` в таблицу `payments`**:
  - Поле `notification_sent INTEGER DEFAULT 0` для защиты от дублирования уведомлений
  - Миграция `migrate_add_notification_sent_to_payments()` добавлена в `db.py`

### Документация
- ✅ **Добавлена документация по упрощению логики подписок**:
  - `docs/SUBSCRIPTION_LOGIC_SIMPLIFICATION_TZ.md` - техническое задание на упрощение
  - `docs/SUBSCRIPTION_LOGIC_AGREED.md` - согласованный вариант реализации
  - `docs/CURRENT_SUBSCRIPTION_PURCHASE_VS_RENEWAL_LOGIC.md` - описание текущей логики
  - `docs/SIMPLIFIED_LOGIC_ANALYSIS.md` - анализ упрощенной логики
  - `docs/SIMPLIFIED_LOGIC_RISKS_REVISED.md` - анализ рисков упрощенной логики
  - `docs/WEBHOOK_RETRY_ANALYSIS.md` - анализ retry webhook'ов
  - `docs/IMPACT_ANALYSIS_EXISTING_SUBSCRIPTIONS.md` - анализ влияния на существующие подписки

## [2.4.13] - 2026-01-16

### Изменено
- ✅ **Очистка проекта от устаревших файлов**:
  - Удалены временные файлы (`subscription_payments_analysis.txt`, `veilbot.db`)
  - Архивированы завершенные документы (9 файлов) в `docs/archive/`
  - Архивированы завершенные скрипты (5 файлов) в `scripts/archive/`

### Добавлено
- ✅ **Документация по безопасности**:
  - Добавлен `docs/SECURITY_AUDIT_REPORT_V2.md` - повторный аудит безопасности после очистки истории Git
  - Добавлен `docs/UNUSED_FILES_ANALYSIS_2026.md` - анализ устаревших файлов проекта
  - Добавлен `docs/RECOMMENDATIONS_BOT_PY_AND_UTILS.md` - рекомендации по bot.py и утилитам отладки

### Очищено
- ✅ **Архивирование завершенных задач**:
  - `SECURITY_AUDIT_REPORT.md`, `SUBSCRIPTION_151_ANALYSIS.md`, `SUBSCRIPTION_DOUBLE_EXTENSION_FIX.md`, `SUBSCRIPTION_OPTIMIZATION_PROPOSAL.md`
  - `CLEANUP_FINAL_REPORT.md`, `CLEANUP_REPORT_2025_12_17.md`, `REFERRAL_TRAFFIC_BONUS_APPLY_LIST.md`
  - `cleanup_project.py`, `normalize_payments_table.py`, `link_subscriptions_payments.py`, `link_subscriptions_to_payments.py`, `show_subscription_payments_table.py`

## [2.4.12] - 2026-01-16

### Исправлено
- ✅ **Подсчет активных и истекших подписок на странице подписок**:
  - Исправлен подсчет статистики: теперь считаются все подписки, а не только текущая страница
  - Статистика корректно работает как с поиском, так и без него

### Добавлено
- ✅ **Поле "архив" для тарифов**:
  - Добавлено поле `is_archived` в таблицу `tariffs` через миграцию
  - Добавлен чекбокс "Архивировать тариф" в форму редактирования тарифа в админке
  - Архивированные тарифы не отображаются при выборе тарифа при покупке или продлении в боте
  - Архивированные тарифы остаются доступны в админке для просмотра и редактирования

### Улучшено
- ✅ **Фильтрация архивных тарифов**:
  - Архивные тарифы автоматически исключаются из меню выбора тарифов (`get_tariff_menu`)
  - Архивные тарифы фильтруются при поиске тарифа по имени и цене (`get_tariff_by_name_and_price`)
  - Все запросы на выбор тарифа при покупке и продлении учитывают статус архивации

## [2.4.11] - 2026-01-16

### Улучшено
- ✅ **Оптимизация проверок в process_subscription_purchase**:
  - Удалена избыточная проверка статуса платежа (сразу после первой проверки)
  - Уменьшено количество запросов к БД на 1 запрос на каждую обработку платежа
  - Улучшена читаемость и поддерживаемость кода

- ✅ **Вынесение констант в класс SubscriptionPurchaseService**:
  - `VERY_RECENT_THRESHOLD = 3600` (1 час) - единообразно используется во всех местах
  - `RECENT_SUBSCRIPTION_THRESHOLD = 1800` (30 минут)
  - `EXPIRES_AT_MATCH_TOLERANCE = 3600` (1 час)
  - Убрана несогласованность порогов (было 300 сек в одних местах, 3600 в других)

- ✅ **Упрощение логики проверки недавно созданных подписок**:
  - Удален недостижимый блок 2 (дублировал блок 1)
  - Упрощена и объединена логика блоков 1 и 3
  - Объединены запросы к БД для подсчета других платежей
  - Уменьшено количество запросов к БД на 1 запрос при обработке платежей с недавно созданными подписками

### Документация
- ✅ Добавлен детальный документ `SUBSCRIPTION_FLOW_WITH_CHECKS.md` с описанием всех 28 проверок
- ✅ Добавлен документ `SUBSCRIPTION_OPTIMIZATION_PROPOSAL.md` с анализом и предложениями по оптимизации
- ✅ Добавлен документ `SUBSCRIPTION_151_ANALYSIS.md` с анализом проблемы подписки #151

## [2.4.10] - 2026-01-16

### Улучшено
- ✅ **Идемпотентность обработки платежей**:
  - Добавлена дополнительная атомарная проверка статуса платежа перед обработкой в `process_subscription_purchase`
  - Это предотвращает двойную обработку платежей при race conditions

- ✅ **Retry механизм для обновления subscription_id**:
  - Метод `update_subscription_id` теперь использует retry механизм с 3 попытками
  - Обрабатывает ошибки "database is locked" и другие временные ошибки БД
  - Улучшено логирование успешных и неуспешных обновлений

- ✅ **Мониторинг платежей без subscription_id**:
  - Добавлена фоновая задача `fix_payments_without_subscription_id()` для автоматического исправления
  - Задача запускается каждые 30 минут и привязывает completed платежи к соответствующим подпискам
  - Автоматически исправляет платежи, у которых не удалось обновить `subscription_id` при обработке

### Исправлено
- ✅ **Обработка ошибок при обновлении subscription_id**:
  - Улучшена обработка ошибок в `_extend_subscription` и `_create_subscription`
  - Добавлено критическое логирование при неудачном обновлении `subscription_id`

## [2.4.9] - 2026-01-16

### Исправлено
- ✅ **Связывание платежей с подписками при продлении**:
  - Добавлен вызов `update_subscription_id` в метод `_extend_subscription` для автоматического связывания платежей с подписками при продлении
  - Исправлена проблема, когда платежи оставались без `subscription_id` после продления подписки

- ✅ **Двойное продление подписок**:
  - Улучшена логика определения покупки/продления: если подписка создана менее 1 часа назад и срок соответствует ожидаемому (`created_at + duration`), это считается покупкой, а не продлением
  - Увеличен порог `VERY_RECENT_THRESHOLD` с 5 минут до 1 часа для предотвращения двойного продления
  - Исправлены подписки 272 и 255, у которых были лишние 30 дней из-за двойного продления

- ✅ **Ручное исправление данных**:
  - Связаны платежи 294 и 295 с подписками 272 и 255 соответственно
  - Исправлены сроки подписок 272 и 255 (убраны лишние 30 дней)

## [2.4.8] - 2026-01-14

### Исправлено
- ✅ **CryptoBot webhook**:
  - Улучшен парсинг `metadata` (поддержка JSON и Python dict строки) для корректного определения платежей подписки

## [2.4.7] - 2026-01-14

### Исправлено
- ✅ **Криптоплатежи за подписки**:
  - Webhook CryptoBot теперь корректно обрабатывает покупку подписки через `SubscriptionPurchaseService`
  - Исправлено чтение `metadata` и корректное отображение `payment_id/user_id/tariff_id` для крипто и YooKassa

- ✅ **Админка /payments**:
  - Исправлено отображение суммы для платежей в USD (показывается `$`, не `₽`)
  - В верхнем блоке “Сумма (completed)” добавлена отдельная сумма для USD (без конвертации)
  - Исправлена ошибка 500 из-за использования `hasattr` в Jinja шаблоне

### Добавлено
- ✅ **Нормализация таблицы payments**:
  - Добавлен скрипт `scripts/normalize_payments_table.py` для нормализации legacy данных и пересборки таблицы `payments` в каноническом виде

## [2.4.6] - 2026-01-11

### Добавлено
- ✅ **Добавлена связь платежей с подписками через subscription_id**:
  - Добавлено поле `subscription_id` в модель `Payment` и таблицу `payments`
  - Реализована автоматическая миграция для добавления колонки `subscription_id`
  - Обновлен `PaymentRepository` для поддержки `subscription_id` (чтение и запись)
  - Обновлен `SubscriptionPurchaseService` для автоматического связывания платежей с подписками
  - Добавлен метод `update_subscription_id` в `PaymentRepository` для обновления связи

- ✅ **Добавлен столбец "Подписка" в таблицу платежей**:
  - В админке на странице `/payments` добавлен столбец с ID подписки
  - Добавлена ссылка на страницу подписок при клике на ID подписки
  - Столбец показывает "—" для платежей без подписки

- ✅ **Страница расхождений подписок**:
  - Добавлена страница `/subscriptions/discrepancies` для анализа расхождений в сроках подписок
  - Обновлено описание формул расчета с учетом разных тарифов

### Изменено
- ✅ **Улучшен расчет расхождений подписок**:
  - Функция `_calculate_subscription_discrepancies` теперь использует `subscription_id` для точной связи платежей с подписками
  - Формулы расчета обновлены для учета разных тарифов в одной подписке
  - Для каждого платежа используется `duration_sec` тарифа этого платежа, а не текущего тарифа подписки
  - Решена проблема смешивания платежей разных подписок при удалении старых подписок

- ✅ **Связывание подписок с платежами**:
  - Связано 15 активных подписок с соответствующими платежами
  - Созданы скрипты для анализа и связывания подписок с платежами

### Исправлено
- ✅ **Исправлено чтение subscription_id из БД**:
  - Обновлен метод `_payment_from_row` для правильного чтения `subscription_id` из таблицы с crypto колонками (позиция 23)
  - Поддержана обратная совместимость с различными схемами таблицы `payments`

## [2.4.5] - 2026-01-11

### Исправлено
- ✅ **Критическое исправление двойного срабатывания при продлении подписок**:
  - Реализовано атомарное SQL-обновление `expires_at` в методе `extend_subscription_by_duration_async`
  - Вычисление нового срока действия теперь происходит непосредственно в БД (`expires_at = expires_at + ?`)
  - Это предотвращает race conditions при одновременной обработке одного платежа несколькими процессами
  - Исправлена проблема, из-за которой подписка продлевалась дважды при одной оплате

- ✅ **Улучшена защита от повторной обработки платежей**:
  - Добавлена проверка статуса `COMPLETED` в Platega webhook перед вызовом `process_subscription_purchase`
  - Улучшены проверки статуса платежа во всех точках входа (webhook'и, фоновые задачи, retry механизмы)
  - Все проверки используют `PaymentStatus.COMPLETED` через enum для надежности
  - Добавлена дополнительная проверка статуса после получения тарифа для защиты от race condition

- ✅ **Исправлены сроки действия подписок**:
  - Исправлено 20 подписок с некорректными сроками действия
  - Сроки пересчитаны на основе фактических платежей (покупка и продления)
  - Учтены реферальные бонусы (+30 дней за каждого реферала с первым платежом)
  - Исправлены подписки с разницей +33 дня (двойное продление) и +3 дня

### Изменено
- ✅ **Улучшена атомарность операций с подписками**:
  - Создан новый метод `extend_subscription_by_duration_async` для атомарного продления подписок
  - Метод использует SQL-вычисления вместо Python-вычислений для предотвращения race conditions
  - Добавлена поддержка `max_expires_at` для защиты от изменения VIP/ручных установок срока

## [2.4.4] - 2026-01-06

### Добавлено
- ✅ **Фильтр по VIP статусу на странице users**:
  - Добавлен выпадающий список для фильтрации пользователей по VIP статусу
  - Доступны опции: "Все пользователи", "Только VIP", "Не VIP"
  - Фильтр сохраняется при поиске и переключении страниц пагинации
  - Обновлены методы `count_users` и `list_users` в `UserRepository` для поддержки фильтрации
  - Фильтр интегрирован с системой live-поиска

## [2.4.3] - 2026-01-06

### Исправлено
- ✅ **Расширен поиск на странице users**:
  - Поиск теперь работает по всем полям: user_id, username, first_name, last_name, email и referral_count
  - Email ищется в таблицах keys, v2ray_keys и payments (как на странице subscriptions)
  - Обновлен placeholder в шаблоне для отражения всех доступных полей поиска

## [2.4.2] - 2026-01-06

### Исправлено
- ✅ **Добавлен серверный поиск с пагинацией на странице subscriptions**:
  - Поиск теперь учитывает пагинацию и работает по всем данным, а не только на текущей странице
  - Поиск работает по ID подписки, ID пользователя, токену подписки, названию тарифа и email пользователя
  - Email ищется в таблицах keys, v2ray_keys и payments
  - Добавлена поддержка параметра `q` в роут `subscriptions_page`
  - Обновлены методы `list_subscriptions` и `count_subscriptions` для поддержки поиска
  - Пагинация теперь сохраняет параметр поиска при переключении страниц
  - Заменен клиентский поиск на серверный через `initLiveSearch`

## [2.4.1] - 2026-01-06

### Исправлено
- ✅ **Исправлено сохранение VIP статуса пользователей**:
  - Использовано делегирование событий для VIP чекбоксов вместо клонирования элементов
  - Добавлена переинициализация обработчиков после обновления таблицы через live-search
  - Добавлено детальное логирование для отладки сохранения VIP статуса
  - Улучшена обработка ошибок при переключении VIP статуса
  - Страница автоматически перезагружается после успешного сохранения для синхронизации данных
- ✅ **Восстановлена функциональность VIP чекбокса**:
  - Восстановлена колонка VIP в таблице пользователей
  - Исправлена обработка 3-значного кортежа (user_id, referral_count, is_vip) в списке пользователей
  - Добавлен обработчик POST `/users/{user_id}/toggle-vip` для переключения VIP статуса
- ✅ **Откат системы поиска**:
  - Откат поиска на всех страницах к состоянию до введения VIP системы (клиентский поиск)
  - Упрощен поиск пользователей - только по user_id
  - Восстановлена работа пагинации на странице subscriptions

## [2.3.13] - 2025-12-19

### Добавлено
- 🛡️ **Абсолютная защита от удаления успешных платежей**:
  - Платежи со статусом `paid` или `completed` теперь НИКОГДА не могут быть удалены
  - Защита реализована на всех уровнях: функция проверки, репозиторий, SQL запросы, админка
  - Все скрипты удаления теперь пропускают защищенные платежи
  - Создан модуль `app/utils/user_deletion_guard.py` для централизованной защиты

### Исправлено
- 🔒 **Защита от случайного удаления пользователей с активными подписками**:
  - Добавлена проверка перед удалением пользователей во всех скриптах
  - Пользователи с активными подписками, успешными платежами или активными ключами не могут быть удалены
  - Все скрипты удаления теперь используют единую систему защиты
  - Улучшена защита в админке при удалении платежей

### Изменено
- 📝 **Обновлена документация**:
  - Добавлен документ `docs/USER_DELETION_PROTECTION.md` с описанием системы защиты
  - Обновлен `docs/SUBSCRIPTION_DOUBLE_EXTENSION_FIX.md`

## [2.3.12] - 2025-12-19

### Исправлено
- 🔧 **Исправлено уведомление администратору при выдаче бесплатного ключа**:
  - Теперь в уведомлении отображается количество созданных ключей обоих типов (V2Ray и Outline)
  - Ранее показывалось только количество V2Ray ключей

## [2.3.11] - 2025-12-18

### Добавлено
- ✅ **Улучшена система резервного копирования БД**:
  - Добавлены уведомления в Telegram об ошибках резервного копирования
  - Реализована многоуровневая политика хранения бэкапов:
    - 48 часовых бэкапов (последние 2 дня)
    - 7 ежедневных бэкапов (последняя неделя) в подкаталоге `daily/`
    - 4 еженедельных бэкапа (последний месяц) в подкаталоге `weekly/`
  - Добавлена автоматическая проверка свежести бэкапов (каждые 30 минут)
  - Добавлена еженедельная проверка целостности бэкапов
  - Улучшена проверка целостности исходной БД и созданных бэкапов
  - Добавлены детальные уведомления администратору при проблемах с бэкапами

### Изменено
- 🔧 **Оптимизировано расписание cron-задач**:
  - Обновлено расписание резервного копирования и мониторинга
  - Добавлены новые задачи проверки свежести и целостности бэкапов
  - Убрано дублирование функций между cron и systemd timers

### Добавлено
- 📝 **Новые скрипты**:
  - `scripts/check_backup_freshness.sh` - проверка свежести бэкапов с уведомлениями
  - `scripts/verify_backup_integrity.sh` - проверка целостности резервных копий
  - `docs/BACKUP_IMPROVEMENTS.md` - документация по улучшениям системы резервного копирования

## [2.3.10] - 2025-12-17

### Исправлено
- 🔧 **Исправлено отображение сумм платежей в скриптах**:
  - Скрипты `get_user_info.py` и `process_payment_manual.py` теперь корректно конвертируют копейки в рубли при выводе
  - Суммы платежей теперь отображаются в рублях (например, 50.00 RUB вместо 5000 RUB)
  - Исправлена проблема с отображением итоговых сумм оплаченных платежей

### Изменено
- 🧹 **Очистка проекта от устаревших файлов**:
  - Архивировано 10 устаревших документов в `docs/archive/`
  - Исправлена зависимость в `scripts/delete_orphaned_keys.py` (восстановлен `compare_keys.py` в `scripts/`)
  - Удалены пустые и поврежденные файлы БД (`veilbot.db`, `vpn.db.corrupted`)
  - Созданы инструменты для анализа и очистки проекта (`analyze_unused_files.py`, `cleanup_project.py`)

### Добавлено
- ✅ **Новые диагностические скрипты**:
  - `scripts/get_user_info.py` - получение полной информации о пользователе
  - `scripts/diagnose_free_subscription.py` - диагностика проблем с бесплатной подпиской
  - `scripts/process_payment_manual.py` - ручная обработка платежей
  - `scripts/check_user_exists.py` - проверка существования пользователя
  - `scripts/list_all_users.py` - список всех пользователей

## [2.3.9] - 2025-12-16

### Добавлено
- ✅ **Улучшена обработка ошибок БД**:
  - Добавлена async версия retry механизма `retry_async_db_operation()` для операций с БД
  - Улучшено логирование ошибок БД с контекстом (operation_name, operation_context)
  - Применено в критическом месте: `PaymentRepository.try_update_status()` теперь использует async retry с детальным логированием
  - Синхронная версия `retry_db_operation()` также улучшена с контекстным логированием
  - Все ошибки логируются с полным контекстом для диагностики

- ✅ **Расширен мониторинг здоровья системы**:
  - Расширен `/healthz` endpoint для проверки всех критичных сервисов
  - Добавлена проверка доступности VPN серверов (Outline, V2Ray) с таймаутом
  - Добавлены метрики производительности (response time для БД и общего времени ответа)
  - Детальная информация о статусе каждого компонента
  - Неблокирующая проверка VPN серверов (не влияет на общий статус health check)

### Изменено
- 🔄 Улучшены предупреждения о SECRET_KEY с инструкциями по генерации в логах при старте
- 🔄 Обновлен документ анализа с информацией о всех исправленных высокоприоритетных проблемах

### Исправлено
- 🔧 Все высокоприоритетные проблемы надежности и безопасности теперь решены
- 🔧 Повышена устойчивость системы при блокировках БД через автоматические retry

## [2.3.8] - 2025-12-16

### Исправлено
- 🔒 **Защита от повторной выдачи бесплатной подписки**:
  - Добавлена проверка в `bot/handlers/subscriptions.py` перед созданием бесплатной подписки через выбор тарифа
  - Добавлена проверка в `SubscriptionService.create_subscription` для предотвращения повторной выдачи бесплатных подписок
  - После успешного создания бесплатной подписки автоматически записывается использование в таблицу `free_key_usage`
  - Теперь система полностью защищена от повторной выдачи бесплатных подписок во всех точках создания

## [2.3.4] - 2025-12-16

### Изменено
- 🔄 **Логика продления подписок V2Ray**:
  - При продлении подписки теперь обновляется `tariff_id` на тариф из последнего платежа
  - Срок действия подписки всегда увеличивается на длительность тарифа, без потери оплаченного времени

### Добавлено
- 🆕 **Управление лимитами трафика на уровне подписки**:
  - Введено поле `subscriptions.traffic_limit_mb` как override над `tariffs.traffic_limit_mb`
  - При создании и продлении подписки лимит трафика подписки и всех её ключей синхронизируется с тарифом
  - Фоновая задача `monitor_subscription_traffic_limits` использует эффективный лимит (подписка → тариф)
  - В `/my_keys` теперь отображается фактический оставшийся трафик подписки

### Исправлено
- 🔧 **Корректное применение лимитов при продлении**:
  - Лимиты трафика обновляются при продлении всегда, в том числе когда новый лимит равен 0 (безлимит)
  - Устранены расхождения между лимитами в подписке и лимитами в ключах подписки

## [2.3.3] - 2025-12-02

### Добавлено
- 🔍 **Живой серверный поиск на всех страницах админки**:
  - Реализован живой поиск (live search) без перезагрузки страницы на всех административных страницах: серверы, тарифы, платежи, пользователи, вебхуки
  - Создан универсальный модуль `live-search.js` для переиспользования логики поиска
  - Поиск работает в реальном времени с debounce 500ms
  - Автоматическое обновление таблиц, статистики и пагинации через AJAX
  - Сохранение поискового запроса в URL для корректной работы пагинации и закладок
  - Кнопка "Сбросить" очищает поле поиска и выполняет живой поиск

### Изменено
- 🔄 **Расширен серверный поиск на всех страницах**:
  - **Серверы**: поиск по ID, названию, стране, протоколу, домену, API URL
  - **Тарифы**: поиск по ID, названию, длительности, цене, трафику
  - **Платежи**: поиск по ID, payment_id, email, user_id, тарифу, провайдеру, статусу
  - **Пользователи**: поиск по ID, email, рефералам
  - **Вебхуки**: поиск по ID, провайдеру, событию, payload, IP, статусу
  - Все страницы теперь используют единый подход к поиску с серверной фильтрацией

## [2.3.2] - 2025-12-02

### Добавлено
- 🔍 **Серверный поиск на странице ключей**:
  - Поиск теперь работает на сервере и учитывает все ключи в базе данных, а не только текущую страницу
  - Поиск по всем столбцам: ID (числовой и с протоколом), email, key_id/v2ray_uuid, server_name, tariff_name, user_id, subscription_id
  - Автоматическая отправка поискового запроса на сервер с debounce 500ms
  - Сохранение поискового запроса в URL для корректной работы пагинации

### Изменено
- 🔄 **Синхронизация подписочных V2Ray ключей с серверами**:
  - `scripts/sync_all_keys_with_servers.py` теперь учитывает лимит `max_keys` на сервере при создании недостающих подписочных ключей
  - Недостающие ключи создаются только на активных серверах, доступных к покупке, и только пока не исчерпан лимит по количеству ключей

## [2.3.0] - 2025-01-21

### Изменено
- 🔄 **Полная переработка системы мониторинга трафика**: Переход на новую архитектуру получения и хранения трафика
  - Использование нового API эндпоинта `GET /api/keys/{key_id}/traffic` для каждого ключа индивидуально
  - Перезапись абсолютных значений трафика вместо расчета дельт через snapshots
  - Единый процесс мониторинга каждые 10 минут для всех активных ключей
  - Упрощенная логика без snapshots и дельт

### Удалено
- 🗑️ **Удалена система лимитов для отдельных ключей**: 
  - Удалена функция `monitor_v2ray_traffic_limits()`
  - Удалены поля `traffic_over_limit_at` и `traffic_over_limit_notified` из таблицы `v2ray_keys`
  - Удалены таблицы `v2ray_usage_snapshots` и `subscription_traffic_snapshots`
  - Лимиты трафика теперь проверяются только на уровне подписок

### Добавлено
- 🆕 **Автоматический сброс трафика при создании/продлении подписки**:
  - Новая функция `reset_subscription_traffic()` для сброса трафика всех ключей подписки
  - Использование нового API эндпоинта `POST /api/keys/{key_id}/traffic/reset`
  - Автоматический сброс при создании новой подписки и при продлении существующей
  - Обнуление трафика в БД для всех ключей подписки и самой подписки

### Исправлено
- 🔧 **Обновлены методы получения трафика в V2RayProtocol**:
  - `get_key_usage_bytes()` теперь использует новый эндпоинт `GET /api/keys/{key_id}/traffic`
  - `reset_key_usage()` использует новый эндпоинт `POST /api/keys/{key_id}/traffic/reset`
  - Удалены все fallback-методы и legacy-код для обратной совместимости

### Миграции БД
- 📊 Добавлены миграции для удаления полей и таблиц:
  - `migrate_remove_traffic_limit_fields_from_v2ray_keys()` - удаляет поля из `v2ray_keys`
  - `migrate_remove_traffic_snapshot_tables()` - удаляет таблицы snapshots

## [2.2.4] - 2025-01-20

### Добавлено
- 🎁 **Реферальные бонусы для подписок**: При наличии активной подписки у реферера, реферальный бонус теперь продлевает подписку (вместо ключа)
  - Приоритет подписке: если у пользователя есть активная подписка, она продлевается на 30 дней
  - Автоматическое продление ключей: при продлении подписки все ключи подписки продлеваются автоматически
  - Обратная совместимость: если подписки нет, работает прежняя логика продления ключей
  - Разные уведомления для подписки и ключа

## [2.2.3] - 2025-11-20

### Исправлено
- 🔧 **Исправлено удаление подписок в админке**: Решена проблема с ошибкой "foreign key mismatch" при удалении подписок
  - Добавлено использование `safe_foreign_keys_off` для безопасного удаления ключей подписки из БД
  - Упрощена обработка ошибок в JavaScript коде (аналогично удалению ключей)
  - Добавлена функция `_compute_subscription_stats` для вычисления статистики подписок
  - Улучшена обработка ошибок в функции `_delete_subscription_internal`

### Добавлено
- 📚 **Документация UI-кита**: Файл `docs/ADMIN_UI_GUIDE.md` с описанием компонентов, утилит и гайдом по разработке интерфейса
- 🛠️ **Фронтенд-пайплайн**: `package.json`, ESLint/Stylelint/Prettier конфиги и npm-скрипты (`npm run lint`, `npm test`)
- 🤖 **Автовыдача бесплатного V2Ray ключа**: при первом `/start` бот готовит бесплатный ключ на NL-сервере (тариф `id=2`) и отправляет пользователю
- 🗂️ **Фильтр бесплатного тарифа**: тариф `id=2` скрыт из списков покупки и продления

### Изменено
- 🧹 `docs/README.md` дополнен инструкцией по установке Node-инструментов и ссылкой на UI-гайд

### Исправлено
- Исправления ошибок

## [2.2.2] - 2025-11-17

### Добавлено
- 👤 **TG ID в таблице ключей**: в столбце «E-mail» теперь дополнительно отображается Telegram ID владельца ключа, что упрощает ручную диагностику и связь с пользователями

## [2.2.1] - 2025-11-17

### Добавлено
- 🔍 **Универсальный поиск в таблицах**: единый JS-хелпер для мгновенного поиска по всем столбцам и `data-*` атрибутам; подключён на страницах `keys`, `servers`, `payments` и `users`

### Изменено
- 🧹 **Страница платежей**: удалены старые формы фильтров, теперь используется только новый глобальный поиск

## [2.2.0] - 2025-11-01

### Исправлено
- 🔧 **Исправление перевыпуска V2Ray ключей**: SNI и shortid теперь корректно обновляются при перевыпуске
  - Использование client_config из ответа create_user API
  - Улучшенный get_user_config с повторными попытками (5 попыток, задержка 1.0-1.5s)
  - Детальное логирование процесса перевыпуска для диагностики
  - Дополнительная задержка перед первым запросом конфигурации
- 📝 **Добавлено детальное логирование**: Логирование SNI и shortid в конфигурациях для отладки

### Добавлено
- 📊 **Комплексный анализ проекта**: Создан файл `PROJECT_ANALYSIS_2025.md` с детальным анализом
  - Выявлены узкие места и рекомендации по оптимизации
  - Приоритизированы задачи по улучшению проекта
  - Проверена настройка ротации логов

## [2.1.0] - 2025-01-02

### Добавлено
- 🎨 **Иконки вместо текста**: Замена всех статусов и протоколов на Material Design иконки во всей админке
  - Статусы (Активен/Истек/Неактивен) теперь отображаются как цветные иконки
  - Протоколы (Outline/V2Ray) отображаются как синие иконки
  - Единый стиль по всей админке для лучшей визуальной согласованности
- 📊 **Оптимизация таблицы серверов**: Уменьшены отступы, убраны пустые места, расширен столбец "Страна"
  - Улучшена компактность и читаемость таблицы
  - Оптимизированы ширины столбцов для лучшего использования пространства
- 📝 **Комплексный анализ проекта**: Добавлен файл `COMPREHENSIVE_ANALYSIS.md`
  - Детальная оценка всех аспектов проекта
  - Рекомендации по дальнейшему развитию

### Изменено
- 🔄 **Модульная архитектура админки**: Полностью разбит монолитный `admin_routes.py` на модули
  - `admin/routes/` - модульные роутеры по функциональности
  - `admin/middleware/` - middleware для auth, audit, bruteforce, error_handler
  - `admin/dependencies/` - зависимости (CSRF, templates)
  - Улучшена поддерживаемость и читаемость кода
- 🗑️ **Удален старый код**: Удален монолитный `admin/admin_routes.py` (2240 строк)

## [2.0.1] - 2025-01-01

### Исправлено
- ⚡ **КРИТИЧНО**: Оптимизация страницы `/keys` - параллельная загрузка V2Ray конфигураций
  - Время загрузки сокращено с ~500 сек до ~10-15 сек для 50 ключей
  - Использование `asyncio.gather()` для параллельных HTTP запросов
  - Улучшение производительности: **30-50x**
- ⚡ **КРИТИЧНО**: Добавлены индексы БД для улучшения производительности запросов
  - Индексы для `tariff_id`, `email`, `created_at`, `status`, `payment_id`
  - Улучшение производительности запросов: **5-10x**
  - Скрипт `scripts/apply_indexes.py` для применения индексов
- 🔒 **КРИТИЧНО**: Улучшена безопасность SQL запросов в webhooks
  - Явная валидация через whitelist для сортировки
  - Документирование безопасности параметризованных запросов
- 🐛 Исправлена проблема с соединениями БД в dashboard
  - Правильное использование контекста `with` для SQLite соединений
- 📝 Добавлена документация анализа и критичных исправлений
  - `admin/ANALYSIS_AND_IMPROVEMENTS.md` - полный анализ админки
  - `admin/CRITICAL_FIXES.md` - отчет об исправлениях

## [2.0.0] - 2025-01-XX

### Добавлено
- 🆕 Поддержка протокола V2Ray VLESS
- 🆕 Система реферальных бонусов
- 🆕 Автоматическая оптимизация памяти
- 🆕 Расширенное логирование безопасности
- 🆕 Система тарифов с гибкими настройками
- 🆕 Выбор страны сервера
- 🆕 Автоматическое продление ключей
- 🆕 Мониторинг состояния серверов

### Изменено
- 🔄 Переход на aiogram 2.25.1
- 🔄 Обновление FastAPI до 0.111.0
- 🔄 Улучшение архитектуры VPN протоколов
- 🔄 Рефакторинг платежного модуля
- 🔄 Оптимизация работы с базой данных

### Исправлено
- 🐛 Исправление утечек памяти
- 🐛 Улучшение обработки ошибок V2Ray API
- 🐛 Исправление проблем с SSL сертификатами
- 🐛 Оптимизация производительности

### Удалено
- 🗑️ Устаревшие VPN протоколы
- 🗑️ Неиспользуемые модули

## [1.5.0] - 2024-12-XX

### Добавлено
- 🆕 Веб-админка на FastAPI
- 🆕 Интеграция с YooKassa
- 🆕 Система управления серверами
- 🆕 Автоматические бэкапы БД
- 🆕 Rate limiting для API

### Изменено
- 🔄 Улучшение безопасности
- 🔄 Оптимизация производительности
- 🔄 Обновление зависимостей

### Исправлено
- 🐛 Исправление уязвимостей безопасности
- 🐛 Улучшение стабильности

## [1.0.0] - 2024-11-XX

### Добавлено
- 🆕 Основной функционал Telegram бота
- 🆕 Поддержка Outline VPN
- 🆕 Базовая система тарифов
- 🆕 SQLite база данных
- 🆕 Система логирования

### Изменено
- 🔄 Первоначальная версия

### Исправлено
- 🐛 Базовые исправления

---

## Примечания по версиям

### Semantic Versioning
- **MAJOR** - Несовместимые изменения API
- **MINOR** - Новая функциональность (обратно совместимо)
- **PATCH** - Исправления ошибок (обратно совместимо)

### Типы изменений
- 🆕 **Добавлено** - Новая функциональность
- 🔄 **Изменено** - Изменения в существующем функционале
- 🐛 **Исправлено** - Исправления ошибок
- 🗑️ **Удалено** - Удаление функциональности
- 🔒 **Безопасность** - Улучшения безопасности
- ⚡ **Производительность** - Улучшения производительности

### Ссылки
- [GitHub Releases](https://github.com/merdocx/veilbot/releases)
- [Issues](https://github.com/merdocx/veilbot/issues)
- [Pull Requests](https://github.com/merdocx/veilbot/pulls) 