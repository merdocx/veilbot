# Техническое задание: VIP пользователи (СОГЛАСОВАНО)

## 1. Общая концепция

Добавить функциональность VIP пользователей, у которых подписки имеют безлимит на срок действия и трафик.

---

## 2. Согласованные решения

### 2.1. Хранение VIP статуса
- **Решение:** Добавить колонку `is_vip INTEGER DEFAULT 0` в таблицу `users`
- **Миграция:** Создать миграцию для добавления колонки (все существующие пользователи = 0)

### 2.2. Механизм безлимита
- **Решение:** Комбинированный подход (Вариант В)
  - **При создании/обновлении подписки для VIP:**
    - Автоматически устанавливать `expires_at = 4102434000` (01.01.2100 00:00 UTC)
    - Автоматически устанавливать `traffic_limit_mb = 0` (0 = безлимит)
  - **При проверке лимитов:**
    - Дополнительно проверять VIP статус пользователя
    - Если VIP → пропускать проверки срока и трафика

### 2.3. Применение к существующим подпискам
- **Решение:** Вариант А
  - При назначении VIP статуса автоматически обновить все активные подписки пользователя:
    - Установить `expires_at = 4102434000`
    - Установить `traffic_limit_mb = 0`

### 2.4. Снятие VIP статуса
- **Решение:** Вариант А
  - Оставить текущие значения (`expires_at` и `traffic_limit_mb`) как есть
  - Не возвращать к исходным значениям

### 2.5. Исключение VIP из фоновых проверок
- **Решение:** Исключать VIP подписки из всех проверок:
  - `auto_delete_expired_keys()` - не удалять ключи VIP подписок
  - `auto_delete_expired_subscriptions()` - не удалять VIP подписки
  - `monitor_subscription_traffic_limits()` - не проверять трафик VIP подписок
  - `notify_expiring_subscriptions()` - не отправлять уведомления VIP подпискам

### 2.6. Отображение в админке
- **На странице `/users`:**
  - Добавить колонку "VIP" с чекбоксом для каждого пользователя
  - Визуальное выделение VIP пользователей (иконка звездочки ⭐ или золотой цвет)
- **На странице `/subscriptions`:**
  - Показывать VIP статус пользователя (иконка или метка)
  - Отображать "Безлимит" для VIP подписок в колонках "Истекает" и "Трафик"

### 2.7. Уведомления пользователю
- **Решение:** Отправлять сообщение пользователю при назначении VIP статуса
- **Текст сообщения:** "🎉 Поздравляем! Вам присвоен VIP статус. Ваши подписки теперь безлимитные по сроку действия и трафику!"

### 2.8. Дополнительные привилегии
- **Решение:** Пока только безлимит (срок и трафик)
- Дополнительные привилегии можно добавить позже

---

## 3. Детальная реализация

### 3.1. База данных

#### Миграция: добавление колонки `is_vip`
```sql
ALTER TABLE users ADD COLUMN is_vip INTEGER DEFAULT 0;
```

**Функция миграции:**
- Проверить наличие колонки
- Если нет → добавить
- Установить значение 0 для всех существующих пользователей

### 3.2. Репозиторий пользователей

**Файл:** `app/repositories/user_repository.py`

**Новые методы:**
- `set_user_vip_status(user_id: int, is_vip: bool) -> None`
- `is_user_vip(user_id: int) -> bool`
- `get_vip_users() -> List[Tuple]`

**Обновить:**
- `get_user()` - включить `is_vip` в SELECT
- `list_users()` - включить `is_vip` в SELECT

### 3.3. Админка: страница пользователей

**Файл:** `admin/routes/users.py`

**Изменения:**
- Добавить колонку "VIP" в таблицу пользователей
- Добавить обработчик POST `/users/{user_id}/toggle-vip` для изменения VIP статуса
- При изменении VIP статуса:
  - Обновить `is_vip` в БД
  - Если установлен VIP → обновить все активные подписки пользователя
  - Отправить уведомление пользователю (если установлен VIP)

**Файл:** `admin/templates/users.html`

**Изменения:**
- Добавить колонку `<th>VIP</th>` в заголовок таблицы
- Добавить ячейку с чекбоксом для каждого пользователя:
  ```html
  <td>
    <input type="checkbox" class="vip-checkbox" 
           data-user-id="{{ user.user_id }}"
           {% if user.is_vip %}checked{% endif %}>
  </td>
  ```
- Добавить визуальное выделение VIP пользователей (CSS класс или иконка)

**Файл:** `admin/static/js/users.js` (если есть) или создать новый

**Функционал:**
- Обработчик изменения чекбокса VIP
- AJAX запрос на `/users/{user_id}/toggle-vip`
- Обновление UI после успешного изменения

### 3.4. Админка: страница подписок

**Файл:** `admin/templates/subscriptions.html`

**Изменения:**
- В колонке "Пользователь" показывать иконку VIP (⭐) если пользователь VIP
- В колонках "Истекает" и "Трафик" для VIP подписок показывать "Безлимит"

**Файл:** `admin/routes/subscriptions.py`

**Изменения:**
- При формировании `subscription_model` проверять VIP статус пользователя
- Если VIP → устанавливать `expiry_state = 'unlimited'` и `traffic_limit_mb = 0`

### 3.5. Создание подписок для VIP

**Файлы:**
- `bot/services/key_creation.py`
- `payments/services/subscription_purchase_service.py`
- Любые другие места создания подписок

**Логика:**
- При создании подписки проверять VIP статус пользователя
- Если VIP:
  - Устанавливать `expires_at = 4102434000`
  - Устанавливать `traffic_limit_mb = 0`

### 3.6. Проверка лимитов

**Файлы для обновления:**

1. **`bot/services/subscription_service.py`**
   - В `generate_subscription_package()`:
     - Проверять VIP статус перед проверкой `expires_at`
     - Проверять VIP статус перед проверкой трафика

2. **`app/repositories/subscription_repository.py`**
   - В методах проверки лимитов добавлять проверку VIP статуса

3. **`bot/services/background_tasks.py`**
   - `auto_delete_expired_keys()`:
     - Исключать ключи VIP подписок из удаления
   - `auto_delete_expired_subscriptions()`:
     - Исключать VIP подписки из удаления
   - `monitor_subscription_traffic_limits()`:
     - Исключать VIP подписки из проверки трафика
   - `notify_expiring_subscriptions()`:
     - Исключать VIP подписки из уведомлений

### 3.7. Уведомления

**Файл:** `admin/routes/users.py`

**При установке VIP:**
```python
from bot.core import get_bot_instance
from bot.utils import safe_send_message

bot = get_bot_instance()
if bot:
    await safe_send_message(
        bot,
        user_id,
        "🎉 Поздравляем! Вам присвоен VIP статус.\n"
        "Ваши подписки теперь безлимитные по сроку действия и трафику!"
    )
```

---

## 4. План реализации

1. ✅ Создать миграцию БД для добавления колонки `is_vip` в `users`
2. ✅ Обновить `UserRepository`:
   - Добавить методы для работы с VIP статусом
   - Обновить существующие методы для включения `is_vip`
3. ✅ Обновить админку `/users`:
   - Добавить колонку VIP с чекбоксом
   - Добавить обработчик изменения VIP статуса
   - Добавить визуальное выделение VIP пользователей
4. ✅ Обновить админку `/subscriptions`:
   - Показывать VIP статус пользователя
   - Отображать "Безлимит" для VIP подписок
5. ✅ Обновить логику создания подписок:
   - Проверять VIP статус при создании
   - Устанавливать безлимитные значения для VIP
6. ✅ Обновить проверки лимитов:
   - Добавить проверку VIP статуса в `subscription_service.py`
   - Обновить репозиторий подписок
7. ✅ Обновить фоновые задачи:
   - Исключить VIP из всех проверок срока и трафика
8. ✅ Добавить уведомления:
   - При назначении VIP статуса

---

## 5. Константы

```python
# В config.py или отдельном файле
VIP_EXPIRES_AT = 4102434000  # 01.01.2100 00:00 UTC
VIP_TRAFFIC_LIMIT_MB = 0  # 0 = безлимит
```

---

## 6. Тестирование

1. Назначить VIP статус пользователю → проверить обновление подписок
2. Создать новую подписку для VIP → проверить безлимитные значения
3. Проверить, что VIP подписки не удаляются фоновыми задачами
4. Проверить, что VIP подписки не проверяются на трафик
5. Снять VIP статус → проверить, что значения остались без изменений
6. Проверить отображение в админке

---

**ТЗ СОГЛАСОВАНО И ГОТОВО К РЕАЛИЗАЦИИ**

