# Исправления использования лимитов трафика - ЗАВЕРШЕНО

**Дата:** 2025-12-17

## ✅ Исправленные проблемы

### 1. Использование удаленных полей (КРИТИЧНО) ✅

**Проблема:** Код пытался использовать поля `traffic_over_limit_at` и `traffic_over_limit_notified`, которые были удалены из таблицы `v2ray_keys`.

**Исправлено:**
- ✅ `bot/services/key_management.py`: Исправлена функция `_resolve_v2ray_usage_metadata()` - убран SELECT удаленных полей
- ✅ `bot/services/key_management.py`: Убраны все INSERT с `traffic_over_limit_at` и `traffic_over_limit_notified` в `v2ray_keys` (5 мест)
- ✅ Убрано использование этих полей из `old_key_data` словарей

### 2. Установка traffic_limit_mb в ключи для подписок ✅

**Проблема:** Лимиты должны храниться только в подписках, но код все еще устанавливал `traffic_limit_mb` в ключи для подписок.

**Исправлено:**
- ✅ `bot/services/key_management.py`: Все INSERT в `keys` теперь устанавливают `traffic_limit_mb = 0` для ключей с `subscription_id` (5 мест)
- ✅ `payments/services/subscription_purchase_service.py`: Убран `traffic_limit_mb` из INSERT в `v2ray_keys` и `keys` (2 места)
- ✅ `bot/services/subscription_service.py`: Убран `traffic_limit_mb` из INSERT в `keys` (1 место)

### 3. UPDATE traffic_limit_mb в ключах ✅

**Проблема:** Код обновлял `traffic_limit_mb` в ключах, хотя лимит должен управляться через подписку.

**Исправлено:**
- ✅ `payments/services/subscription_purchase_service.py`: Убраны UPDATE `traffic_limit_mb` в `v2ray_keys` и `keys` (1 место)
- ✅ `bot/services/key_management.py`: Убран backfill `traffic_limit_mb` после создания ключа

## 📋 Измененные файлы

1. **bot/services/key_management.py**
   - Исправлена функция `_resolve_v2ray_usage_metadata()` 
   - Убраны удаленные поля из 5 INSERT в `v2ray_keys`
   - Все INSERT в `keys` теперь устанавливают `traffic_limit_mb = 0` для подписок

2. **payments/services/subscription_purchase_service.py**
   - Убран `traffic_limit_mb` и `expiry_at` из INSERT в `v2ray_keys`
   - Убран `traffic_limit_mb` и `expiry_at` из INSERT в `keys`
   - Убраны UPDATE `traffic_limit_mb` в ключах

3. **bot/services/subscription_service.py**
   - Убран `traffic_limit_mb` и `expiry_at` из INSERT в `keys`

## ✅ Правильная архитектура

Теперь код соответствует правильной архитектуре:

1. **Лимиты хранятся только в подписках:**
   - `subscriptions.traffic_limit_mb` - индивидуальный лимит подписки
   - `tariffs.traffic_limit_mb` - лимит из тарифа (fallback)

2. **Ключи не хранят лимиты:**
   - Для ключей с `subscription_id` устанавливается `traffic_limit_mb = 0`
   - Лимит берется из подписки при проверках

3. **Использование трафика в ключах:**
   - `v2ray_keys.traffic_usage_bytes` - используется для агрегации трафика подписки
   - Это правильно, так как каждый ключ отслеживает свой трафик

4. **Удаленные поля больше не используются:**
   - `traffic_over_limit_at` и `traffic_over_limit_notified` удалены из `v2ray_keys`
   - Для подписок используются аналогичные поля в `subscriptions`

## ⚠️ Legacy код

Следующие места могут содержать `traffic_limit_mb` для legacy ключей без подписки:
- `bot/services/background_tasks.py` - создание standalone ключей
- `bot.py` - старый код создания ключей

Эти места могут быть исправлены позже, но не критичны, так как касаются legacy функциональности.

## 🎯 Результат

Все критические проблемы исправлены. Код теперь корректно работает с новой архитектурой, где лимиты управляются через подписки, а не через ключи.

