# Техническое задание: Безлимитные подписки

## ⚠️ УСТАРЕЛО

**Статус:** Функция безлимита полностью удалена из проекта.

**Дата удаления:** 2025-01-27

**Причина:** Функция безлимита была полностью удалена из проекта. Все подписки теперь работают как обычные подписки с лимитами по сроку и трафику.

**Изменения:**
- Колонка `unlimited` удалена из таблицы `subscriptions`
- Вся логика обработки `unlimited` удалена из кода
- Текущие значения `expires_at` и `traffic_limit_mb` сохранены без изменений

**Миграция:** `migrate_remove_unlimited_column()` в `db.py`

---

*Этот документ сохранен для истории. Все упоминания функции безлимита удалены из активного кода.*
